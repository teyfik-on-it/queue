version: '3.7'

services:
  app:
    build:
      context: .
    volumes:
      - ./src:/app/src
    ports:
      - 9229:9229
      - ${PORT}:${PORT}
    command: npm run start:dev
    env_file:
      - .env
